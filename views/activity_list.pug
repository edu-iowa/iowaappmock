extends layout 

block content 
    h1= title
    h2= level.name 
    h3= unit.name

    ul 
        each activity in activity_list
            li 
            a(href=activity.url) #{activity.no}
    